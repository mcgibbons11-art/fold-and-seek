/**
 * Every authored prop in The Curiosity Shop (§10.3). It lives in
 * `@foldseek/map-data` because the dedicated server derives its navigation
 * blockers, its focus boxes and its object registry from the same list; this
 * file is the client's name for it.
 */

export {
  countByFamily,
  PRACTICAL_PLACEMENTS,
  SHOP_PLACEMENTS,
  type PracticalLight,
  type PropFamily,
  type PropPlacement,
  type PropVariant,
} from "@foldseek/map-data";
